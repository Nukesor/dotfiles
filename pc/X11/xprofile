{%- if profile == "desktop" %}
amixer -c 0 cset numid=22 'Headphones' &
{% endif -%}

feh --no-fehbg --bg-scale ~/.local/bg.jpg &

exec ~/.bin/keyboard-layout en &

# Load base monitor layout on start
if [ -f ~/.bin/base.sh ]; then
    exec ~/.bin/base.sh &
fi
