{%- if profile == "desktop" %}
~/.bin/fix_xonar_output
{% endif -%}

export PATH=~/.cache/cargo/bin:$PATH
export PATH=~/.bin:$PATH

# Load zshenv
[ -f ~/.config/zsh/.zshenv ] && . ~/.config/zsh/.zshenv

# Load keyboard layout
exec ~/.bin/keyboard-layout en &

# Set background pic
feh --no-fehbg --bg-scale ~/.local/bg.jpg &
